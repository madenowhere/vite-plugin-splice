# Build output (regenerated by `pnpm build` / `pnpm publish`)
dist/

# Dependencies
node_modules/

# pnpm logs / debug
pnpm-debug.log*

# OS noise
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# Editor noise
.idea/
.vscode/
*.swp

# Never commit font binaries — this plugin operates on fonts as inputs
# but ships none. Test fixtures should live outside the repo.
*.woff
*.woff2
*.ttf
*.otf
*.eot
